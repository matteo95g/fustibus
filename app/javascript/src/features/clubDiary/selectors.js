export const missionsState = (state) => state.clubDiary.current.missions;

export const missionStatus = (state) => state.clubDiary.createMissionStatus;

export const updateMissionStatus = (state) => state.clubDiary.updateMissionStatus;

export const deleteMissionStatus = (state) => state.clubDiary.deleteMissionStatus;
